import { ComprobantePipe } from './comprobante.pipe';

describe('ComprobantePipe', () => {
  it('create an instance', () => {
    const pipe = new ComprobantePipe();
    expect(pipe).toBeTruthy();
  });
});
