import { BotonesComprobantePipe } from './botones-comprobante.pipe';

describe('BotonesComprobantePipe', () => {
  it('create an instance', () => {
    const pipe = new BotonesComprobantePipe();
    expect(pipe).toBeTruthy();
  });
});
