import { RechazadoComprobantePipe } from './rechazado-comprobante.pipe';

describe('RechazadoComprobantePipe', () => {
  it('create an instance', () => {
    const pipe = new RechazadoComprobantePipe();
    expect(pipe).toBeTruthy();
  });
});
